//This is at the very bottom of the page, a small section with a link to the real page
const LinkToRealPage = () => {
    //No props or anything else needed
    return (
        <div className="link-to-real-page">
            <h2>And <a href="https://www.gov.uk/">this</a> is the link to the real page!</h2>
        </div>
    )
};

export default LinkToRealPage;